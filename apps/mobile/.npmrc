# Metro requires a flat node_modules structure (hoisted),
# but we scope this to apps/mobile only and limit what floats up
# to the workspace root to prevent duplicate React instances.
node-linker=hoisted
public-hoist-pattern[]=*
